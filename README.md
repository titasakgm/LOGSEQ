# LOGSEQ

Create a new PAT token in titasakgm@github.com with PAT

Then issue command
$ git config --global credential.helper manager

Try push source again
$ git add .
$ git commit -m "After generate PAT TOKEN"
$ git push -u origin main


